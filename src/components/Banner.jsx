// Setting up the react banner at the top of the page //

function Banner() {
       return(
        <div>
            <h1>"Orbit Report"</h1>
            <p>Click on the buttons to see the satellites in that orbit type function.</p>
        </div>
       );
    };
    
    export default Banner;
